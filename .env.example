GEMINI_API_KEY=
GROQ_API_KEY=
# Leave empty for dev (server auto-generates), or run:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FILE_ENCRYPTION_KEY=
ENABLE_HTTPS_REDIRECT=false
